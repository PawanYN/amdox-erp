import { CurrentUser } from "./types";

// TODO(Task 5 — backend): replace with the authenticated session user
// once GET /api/auth/me (or equivalent) is wired up.
export const currentUser: CurrentUser = {
  employeeId: "EMP-102",
  name: "Riya Patel",
  role: "Manager",
};
